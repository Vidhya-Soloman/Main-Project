<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:paddingStart="20dp"
    android:paddingEnd="20dp"
    android:paddingBottom="40dp"
    android:fitsSystemWindows="true"
    android:paddingTop="30dp">

    <!-- Title TextView -->
    <TextView
        android:id="@+id/title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="20dp"
        android:text="Ebill"
        android:textAlignment="center"
        android:textSize="60sp"
        android:textStyle="bold"
        android:textColor="#2F4F4F" />

    <!-- Box Container (CardView) for other views -->
    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        app:cardCornerRadius="16dp"
    app:cardElevation="8dp"
    app:contentPadding="20dp">
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center">

        <!-- New User TextView -->
        <TextView
            android:id="@+id/new_user_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="New user?"
            android:textSize="18sp"
            android:textColor="#808080"
            android:layout_marginBottom="10dp" />

        <!-- Sign Up Button with blue color and rounded corners -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/register_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Sign Up"
            android:textColor="#FFFFFF"
            android:padding="12dp"
            android:backgroundTint="#007BFF"
            android:layout_marginBottom="20dp"
            app:cornerRadius="24dp" /> <!-- Rounded corners -->

        <!-- Already have an account? TextView above the Login button -->
        <TextView
            android:id="@+id/already_account_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Already have an account?"
            android:textSize="18sp"
            android:textColor="#808080"
            android:layout_marginTop="20dp"
            android:layout_marginBottom="10dp" />

        <!-- Login Button with green color and rounded corners -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/login"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Login"
            android:textColor="#FFFFFF"
            android:padding="12dp"
            android:backgroundTint="#28A745"
            app:cornerRadius="24dp" /> <!-- Rounded corners -->

    </LinearLayout>
</androidx.cardview.widget.CardView>

    </LinearLayout>
